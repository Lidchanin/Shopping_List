package com.lidchanin.crudindiploma.database.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.query.Query;
import org.greenrobot.greendao.query.QueryBuilder;

import com.lidchanin.crudindiploma.database.Product;

import com.lidchanin.crudindiploma.database.UsedProduct;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "USED_PRODUCT".
*/
public class UsedProductDao extends AbstractDao<UsedProduct, Long> {

    public static final String TABLENAME = "USED_PRODUCT";

    /**
     * Properties of entity UsedProduct.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Quantity = new Property(1, double.class, "quantity", false, "QUANTITY");
        public final static Property Unit = new Property(2, boolean.class, "unit", false, "UNIT");
        public final static Property IsPurchased = new Property(3, boolean.class, "isPurchased", false, "IS_PURCHASED");
        public final static Property ProductId = new Property(4, Long.class, "productId", false, "PRODUCT_ID");
        public final static Property ShoppingListId = new Property(5, long.class, "shoppingListId", false, "SHOPPING_LIST_ID");
    }

    private DaoSession daoSession;

    private Query<UsedProduct> shoppingList_UsedProductsQuery;

    public UsedProductDao(DaoConfig config) {
        super(config);
    }
    
    public UsedProductDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"USED_PRODUCT\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"QUANTITY\" REAL NOT NULL ," + // 1: quantity
                "\"UNIT\" INTEGER NOT NULL ," + // 2: unit
                "\"IS_PURCHASED\" INTEGER NOT NULL ," + // 3: isPurchased
                "\"PRODUCT_ID\" INTEGER," + // 4: productId
                "\"SHOPPING_LIST_ID\" INTEGER NOT NULL );"); // 5: shoppingListId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"USED_PRODUCT\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, UsedProduct entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindDouble(2, entity.getQuantity());
        stmt.bindLong(3, entity.getUnit() ? 1L: 0L);
        stmt.bindLong(4, entity.getIsPurchased() ? 1L: 0L);
 
        Long productId = entity.getProductId();
        if (productId != null) {
            stmt.bindLong(5, productId);
        }
        stmt.bindLong(6, entity.getShoppingListId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, UsedProduct entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindDouble(2, entity.getQuantity());
        stmt.bindLong(3, entity.getUnit() ? 1L: 0L);
        stmt.bindLong(4, entity.getIsPurchased() ? 1L: 0L);
 
        Long productId = entity.getProductId();
        if (productId != null) {
            stmt.bindLong(5, productId);
        }
        stmt.bindLong(6, entity.getShoppingListId());
    }

    @Override
    protected final void attachEntity(UsedProduct entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public UsedProduct readEntity(Cursor cursor, int offset) {
        UsedProduct entity = new UsedProduct( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getDouble(offset + 1), // quantity
            cursor.getShort(offset + 2) != 0, // unit
            cursor.getShort(offset + 3) != 0, // isPurchased
            cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4), // productId
            cursor.getLong(offset + 5) // shoppingListId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, UsedProduct entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setQuantity(cursor.getDouble(offset + 1));
        entity.setUnit(cursor.getShort(offset + 2) != 0);
        entity.setIsPurchased(cursor.getShort(offset + 3) != 0);
        entity.setProductId(cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4));
        entity.setShoppingListId(cursor.getLong(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(UsedProduct entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(UsedProduct entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(UsedProduct entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "usedProducts" to-many relationship of ShoppingList. */
    public List<UsedProduct> _queryShoppingList_UsedProducts(long shoppingListId) {
        synchronized (this) {
            if (shoppingList_UsedProductsQuery == null) {
                QueryBuilder<UsedProduct> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ShoppingListId.eq(null));
                shoppingList_UsedProductsQuery = queryBuilder.build();
            }
        }
        Query<UsedProduct> query = shoppingList_UsedProductsQuery.forCurrentThread();
        query.setParameter(0, shoppingListId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getProductDao().getAllColumns());
            builder.append(" FROM USED_PRODUCT T");
            builder.append(" LEFT JOIN PRODUCT T0 ON T.\"PRODUCT_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected UsedProduct loadCurrentDeep(Cursor cursor, boolean lock) {
        UsedProduct entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Product product = loadCurrentOther(daoSession.getProductDao(), cursor, offset);
        entity.setProduct(product);

        return entity;    
    }

    public UsedProduct loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<UsedProduct> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<UsedProduct> list = new ArrayList<UsedProduct>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<UsedProduct> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<UsedProduct> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
